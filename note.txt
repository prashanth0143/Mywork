given range 1 to n ;   
   0 to n  == index = nums[value]
    1 to n == index = nums[value] - 1
     Cyclic sort'

1. Bublle sort       
2.selection sort
3.inserction sort

we add anything with char it returns int accivalue

we add anything wiht a + 0 = result convert to char a
if we substract '0' with char it gives the int form 
